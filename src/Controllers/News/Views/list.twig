{% extends "layout.twig" %}
{% block content %}
    {% for news in news %}
        <h3><a href="news/{{ news.id }}">{{ news.titre }}</a></h3>
        <p>{{ news.contenu }}</p>
        <p>{{ news.auteur }}</p>
    {% endfor %}
{% endblock %}